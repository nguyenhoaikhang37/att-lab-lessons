type ButtonGenericProps = {
  countValue: number;
  countHistory: number[];
};

const ButtonGeneric = ({ countValue, countHistory }: ButtonGenericProps) => {
  return <div>ButtonGeneric</div>;
};

export default ButtonGeneric;
